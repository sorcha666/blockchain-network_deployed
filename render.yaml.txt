services:
  - type: web
    name: blockchain-network
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn blockchain_network:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0